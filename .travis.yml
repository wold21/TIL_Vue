language: node_js 
os: linux
node_js:
  - lts:*
install:
  - yarn install
script:
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $TIL_TOKEN
  local_dir: build/
  keep_history: true
  on:
    branch: main
  target_branch: gh-pages
  repo: wold21/TIL_Vue